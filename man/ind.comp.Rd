% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind.comp.R
\name{ind.comp}
\alias{ind.comp}
\title{takes r from individual item with outcome to estimate the r between composite of those items and outcome}
\usage{
ind.comp(item_outcome, inter_item)
}
\arguments{
\item{item_outcome}{a vector of correlations between each item and the outcome}

\item{inter_item}{a symmetric correlation matrix of inter-item correlations}
}
\description{
estimation is based on:
use with r_composite, y = ( Sum from i=1 to n of r_i,y) / sqrt( n + 2 × ( Sum of r_i,j over all i<j ) )
}
\examples{

# example usage
# correlations of individual items with the outcome
item_outcome <- c(0.5, 0.6, 0.4)

# symmetric inter-item correlation matrix
inter_item <- matrix(
  c(1.0, 0.3, 0.2,
    0.3, 1.0, 0.4,
    0.2, 0.4, 1.0),
  nrow = 3, byrow = TRUE
)

# estimate composite correlation
ind.comp(item_outcome, inter_item)

}
